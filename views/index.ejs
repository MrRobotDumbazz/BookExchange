<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>
    <%= title %>
  </title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.min.css" rel="stylesheet">
  <style>
    .card:hover {
      transform: scale(1.05);
      transition: transform 0.3s;
    }

    .bg-primary-custom {
      background: url('https://example.com/your-banner-image.jpg') no-repeat center center;
      background-size: cover;
      color: #fff;
    }

    .banner-text {
      text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.6);
    }

    .footer {
      background-color: #f8f9fa;
      padding: 2rem 0;
      text-align: center;
    }

    .footer a {
      text-decoration: none;
    }

    .footer a:hover {
      text-decoration: underline;
    }
  </style>
</head>

<body>
  <!-- Include Navbar -->
  <%- include('navbar') %>

    <!-- Главный баннер -->
    <section class="bg-primary-custom text-center py-5">
      <div class="container">
        <h1 class="banner-text">
          <%= bannerTitle %>
        </h1>
        <p class="lead banner-text">
          <%= bannerText %>
        </p>
        <a href="#books" class="btn btn-secondary btn-lg">Начать исследование</a>
      </div>
    </section>

    <!-- Раздел книг -->
    <section id="books" class="py-5">
      <div class="container">
        <h2 class="text-center">Обмен Книгами</h2>
        <div class="row">
          <% books.forEach(function(book) { %>
            <div class="col-md-4 mb-4">
              <div class="card">
                <img src="<%= book.image %>" class="card-img-top" alt="<%= book.title %>">
                <div class="card-body">
                  <h5 class="card-title">
                    <%= book.title %>
                  </h5>
                  <p class="card-text">
                    <%= book.description %>
                  </p>
                  <a href="#" class="btn btn-primary">Обменять</a>
                </div>
              </div>
            </div>
            <% }); %>
        </div>
      </div>
    </section>

    <!-- Раздел обсуждений -->
    <section id="discussions" class="py-5 bg-light">
      <div class="container">
        <h2 class="text-center">Обсуждения</h2>
        <p class="text-center">Присоединяйтесь к обсуждениям любимых книг с другими читателями.</p>
        <div class="d-grid gap-2 col-6 mx-auto">
          <a href="#" class="btn btn-secondary btn-lg">Перейти к обсуждениям</a>
        </div>
      </div>
    </section>

    <!-- Раздел полок -->
    <section id="shelves" class="py-5">
      <div class="container">
        <h2 class="text-center">Мои Полки</h2>
        <p class="text-center">Создайте свои собственные книжные полки и делитесь ими с другими.</p>
        <div class="d-grid gap-2 col-6 mx-auto">
          <a href="#" class="btn btn-secondary btn-lg">Создать Полку</a>
        </div>
      </div>
    </section>

    <!-- Рекомендации -->
    <section id="recommendations" class="py-5 bg-light">
      <div class="container">
        <h2 class="text-center">Рекомендации</h2>
        <p class="text-center">Получите персональные рекомендации книг на основе ваших интересов.</p>
        <div class="d-grid gap-2 col-6 mx-auto">
          <a href="#" class="btn btn-secondary btn-lg">Получить Рекомендации</a>
        </div>
      </div>
    </section>

    <!-- Подключение Bootstrap -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script>

    <!-- Футер -->
    <%- include('footer') %>
</body>

</html>